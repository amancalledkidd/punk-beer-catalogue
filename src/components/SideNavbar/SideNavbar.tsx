import SearchBox from "../SearchBox/SearchBox"

const SideNavbar = () => {
    return (
        <div className="side-navbar">
            <SearchBox />
        </div>
    )
}

export default SideNavbar